<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.posadskiy.auth</groupId>
    <artifactId>auth-service</artifactId>
    <version>0.1.6</version>
    <packaging>pom</packaging>
    <modules>
        <module>auth-service-api</module>
        <module>auth-service-core</module>
        <module>auth-service-web</module>
    </modules>

    <parent>
        <groupId>io.micronaut.platform</groupId>
        <artifactId>micronaut-parent</artifactId>
        <version>4.5.0</version>
    </parent>
    <properties>
        <packaging>jar</packaging>
        <jdk.version>21</jdk.version>
        <release.version>21</release.version>
        <micronaut.version>4.5.0</micronaut.version>
        <lombok.version>1.18.34</lombok.version>
        <micronaut.aot.enabled>false</micronaut.aot.enabled>
        <micronaut.aot.packageName>auth.service.aot.generated</micronaut.aot.packageName>
        <micronaut.runtime>netty</micronaut.runtime>
        <exec.mainClass>com.posadskiy.auth.web.Application</exec.mainClass>
        
        <auth-service-api.version>0.1.3</auth-service-api.version>
        <auth-service-core.version>0.1.3</auth-service-core.version>
        <auth-service-web.version>0.1.3</auth-service-web.version>
        
        <logstash-logback-encoder.version>7.4</logstash-logback-encoder.version>
        <jakarta.validation-api.version>3.1.0</jakarta.validation-api.version>
        <spring-security-crypto.version>6.3.0</spring-security-crypto.version>
        <commons-lang3.version>3.14.0</commons-lang3.version>
    </properties>

    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo.maven.apache.org/maven2</url>
        </repository>
    </repositories>

    <dependencyManagement>
    <dependencies>
        <dependency>
                <groupId>com.posadskiy.auth</groupId>
                <artifactId>auth-service-api</artifactId>
                <version>${auth-service-api.version}</version>
        </dependency>
        <dependency>
                <groupId>com.posadskiy.auth</groupId>
                <artifactId>auth-service-core</artifactId>
                <version>${auth-service-core.version}</version>
        </dependency>
        <dependency>
                <groupId>com.posadskiy.auth</groupId>
                <artifactId>auth-service-web</artifactId>
                <version>${auth-service-web.version}</version>
        </dependency>
            
        <dependency>
            <groupId>net.logstash.logback</groupId>
            <artifactId>logstash-logback-encoder</artifactId>
                <version>${logstash-logback-encoder.version}</version>
        </dependency>

            <!-- Validation -->
        <dependency>
                <groupId>jakarta.validation</groupId>
                <artifactId>jakarta.validation-api</artifactId>
                <version>${jakarta.validation-api.version}</version>
        </dependency>
            <!-- Validation -->

        <!-- Security -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
                <version>${spring-security-crypto.version}</version>
        </dependency>
        <!-- Security -->

        <!-- Utilities -->
        <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
        </dependency>
            <!-- Utilities -->
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
        </dependency>
    </dependencies>

</project>
